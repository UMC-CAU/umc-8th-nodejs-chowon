{
  "compilerOptions": {
    /* 언어 및 환경 설정 */
    "target": "ES2022",                           // 출력할 JavaScript 버전 (최신 Node.js 지원)
    "module": "ESNext",                           // ES modules 사용 (package.json의 "type": "module"과 일치)
    "moduleResolution": "Node",                   // Node.js 스타일 모듈 해석
    "lib": ["ES2022"],                           // 사용할 라이브러리 (최신 ECMAScript 기능)
    
    /* 점진적 마이그레이션 설정 - TODO: 마이그레이션 완료 후 제거 */
    "allowJs": true,                             // JavaScript 파일 허용 (기존 .js 파일과 함께 작업 가능)
    "checkJs": false,                            // JavaScript 파일은 타입 체크하지 않음 (선택적으로 나중에 활성화)
    "incremental": false,                        // 증분 컴파일 (빠른 재컴파일) - 임시 비활성화
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tsbuildinfo", // 빌드 정보 캐시 위치
    
    /* 출력 설정 */
    "outDir": "./dist",                          // 컴파일된 파일 출력 디렉터리
    "rootDir": "./src",                          // 소스 파일 루트 디렉터리 (src 폴더)
    "removeComments": true,                      // 컴파일된 파일에서 주석 제거
    "sourceMap": true,                           // 디버깅을 위한 소스맵 생성
    
    /* 타입 체킹 설정 */
    "strict": true,                              // 모든 엄격한 타입 체킹 옵션 활성화
    "noImplicitAny": true,                       // 암시적 any 타입 금지
    "strictNullChecks": true,                    // null/undefined 엄격 체크
    "strictFunctionTypes": true,                 // 함수 타입 엄격 체크
    "noImplicitReturns": true,                   // 모든 코드 경로에서 반환값 필요
    "noFallthroughCasesInSwitch": true,         // switch문에서 fallthrough 방지
    "noUncheckedIndexedAccess": true,           // 인덱스 접근 시 undefined 가능성 체크
    
    /* 모듈 설정 */
    "esModuleInterop": true,                     // CommonJS와 ES modules 간 호환성
    "allowSyntheticDefaultImports": true,        // default import 허용 (esModuleInterop과 함께 사용)
    "forceConsistentCasingInFileNames": true,    // 파일명 대소문자 일관성 강제
    "resolveJsonModule": true,                   // JSON 파일 import 허용
    
    /* 고급 설정 */
    "skipLibCheck": true,                        // 라이브러리 타입 정의 파일 체크 건너뛰기 (빌드 속도 향상)
    "declaration": true,                         // .d.ts 파일 생성
    "declarationMap": true,                      // 선언 파일 소스맵 생성
    "experimentalDecorators": true,              // 데코레이터 실험적 지원 (향후 사용 가능)
    "emitDecoratorMetadata": true,              // 데코레이터 메타데이터 생성
    
    /* 경로 설정 */
    "baseUrl": "."                               // 상대 경로의 기준점
  },
  
  /* 포함/제외 설정 */
  "include": [
    "src/**/*.ts"                                // src 폴더 내 TypeScript 파일만 포함
  ],
  "exclude": [
    "node_modules",                              // node_modules 제외
    "dist",                                      // 빌드 출력 폴더 제외
    "**/*.test.ts",                             // 테스트 파일 제외 (별도 설정 가능)
    "**/*.test.js",                             // JavaScript 테스트 파일 제외
    "src/generated/**/*",                        // Prisma 생성 파일 제외
    "src/**/*.js"                               // src 폴더 내 JavaScript 파일 제외 - TODO: 마이그레이션 완료 후 제거
  ],
  
  /* Node.js 관련 설정 */
  "ts-node": {
    "esm": true,                                 // ts-node에서 ES modules 지원
    "experimentalSpecifierResolution": "node"    // Node.js 스타일 모듈 해석
  }
}
