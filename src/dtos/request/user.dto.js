export const AddMissionDto = (body) => {
    return {
        missionId: body.missionId,
    };
}

export const completeMissionRequestDTO = (body) => {
    return {
        missionId: body.missionId,
    };
}